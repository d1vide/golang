global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: tasks
    static_configs:
      # host.docker.internal resolves to the host machine from inside Docker.
      # On Linux replace with your machine's LAN IP if host.docker.internal is unavailable.
      - targets: ["host.docker.internal:8082"]